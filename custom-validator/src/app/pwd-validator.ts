// import { FormGroup, ValidationErrors, ValidatorFn, AbstractControl } from "@angular/forms"

// export function passwordValidator(passwordOneControl: string, reenterPasswordControl: string): ValidatorFn {
//     return (form: AbstractControl): ValidationErrors => {
//         const passwordOne = passwordOneControl
//         const passwordTwo = reenterPasswordControl

//         if(!passwordOne || !passwordTwo){
            
//         }
  
//         if(passwordOne )
//     }

    
// }

